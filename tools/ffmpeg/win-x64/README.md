# FFmpeg Windows x64 Placeholder

Place `ffmpeg.exe` and `ffprobe.exe` in this directory when preparing a local
development or distribution bundle.

Do not rely on FFmpeg installed globally or available through `PATH`.
